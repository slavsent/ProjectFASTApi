version: '3.8'

services:
  test_restoran_app:
    container_name: test_restoran_app
    build:
      context: .
      dockerfile: Dockerfile

    command: "pytest -vv"
    networks:
#      - restoran_network
      - test_network
      - another_network

networks:
  test_network:
    external:
      name: restoran_network
  another_network:
    name: another_network
